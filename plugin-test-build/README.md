# This subproject is only used for test

To test the plugin from command line from the root project you can do

./gradlew :plugin-test-build:{pluginTaskName}

for example

./gradlew :plugin-test-build:printCatalogContent